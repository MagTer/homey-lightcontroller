# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | Planning the configuration model in S02. | architecture | Using Zod provides both runtime validation and TypeScript type inference from a single source of truth, ensuring the AppConfig object is always valid and strongly typed. | Zod for domain model and configuration validation. | Reduces boilerplate, provides clear validation errors, and keeps types in sync with validation logic. | true | agent |
| D002 | Planning M002 (Hardening & Resilience). Could have collapsed into one or two slices since each edit is sub-day, but value of independent test surfaces outweighs the planning tax. | milestone-planning | M002 slice decomposition granularity: one slice per requirement vs. bundling into 1-2 slices. | Four slices, one per requirement (R008 → S01, R009 → S02, R010 → S03, R011 → S04). | Each requirement touches a different file (package.json, Reconciler.ts, app.ts, PhaseEngine.ts) with independent verification surfaces (build/runtime vs. three different unit-test targets). Keeping them split gives clean per-requirement traceability, lets each fail or succeed independently, and avoids a single PR that touches three subsystems at once. The work is small enough that bundling would not save meaningful overhead. | yes — if execution shows excessive ceremony per slice, future hardening milestones can bundle similar surgical edits. | agent |
| D003 | Planning M002. Research recommended this exact order; ratifying it as a structural decision. | milestone-planning | M002 slice ordering: which hardening point ships first. | S01 = Node 20 downgrade first, then S02 (retry), S03 (config validation), S04 (PhaseEngine tiebreak) in parallel-safe order. | Risk-first principle: S01 has the highest external risk (a transitive dep using Node 22-only APIs would block the entire deployment fix). Locking the toolchain first means S02–S04 verify under the real target engine. S02 ships next because it has the most behavioral risk (timing semantics in a hot path); S03 introduces a new boundary check; S04 is the smallest scope and lowest risk. | no — S01 must lead so downstream slices verify on the target Node version. | agent |
| D004 | Planning M002. Reading app.ts confirmed onInit currently only registers flow cards — there is no engine startup to gate. The CONTEXT note "engine will not start" is forward-looking; M002 establishes the contract, a future milestone wires the engine to it. | scope-exclusion | Whether R010 (eager config validation) should also wire up engine startup in onInit during M002. | No — S03 only adds the eager parse + critical-log + safe-skip behavior. Actual engine instantiation in onInit is out of scope for M002. | M002-CONTEXT explicitly scopes the milestone as "config boundary hardening" plus three other surgical fixes; engine startup wiring is a larger surface that warrants its own milestone with integration tests. The audit point R010 addresses is "validated configuration contract at the boundary" — this is fully achieved by parsing eagerly and refusing to proceed on invalid input, even when "proceed" currently means "register flow cards." | yes — if a later milestone discovers the boundary check needs to be tightened (e.g. throw vs. log+skip), it can revise the contract here. | agent |
